/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package automenta.spacenet.plugin.web;

import automenta.spacenet.plugin.xml.XMLAgent;

/**
 *
 * @author seh
 */
abstract public class HTMLAgent extends XMLAgent {

//    public HTMLAgent(URI uri) throws Exception {
//        super(uri);
//    }
//
//    @Override protected void onNode(Node node) {
//        if (node.getNodeName().equals("img")) {
//            onImage(node, node.getAttributes().getNamedItem("src").getTextContent());
//        }
//        String text = node.getTextContent();
//        if (text!=null)
//            if (text.length() > 1)
//                onText(node, text);
//    }
//
//    abstract protected void onText(Node textNode, String text);
//    abstract protected void onImage(Node imgNode, String url);
//

}
